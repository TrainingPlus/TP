rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    match /cv/{studentId}/{fileName} {
      allow read, write: if request.auth != null;
    }

    match /chat/{threadId}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
